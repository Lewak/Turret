#include <Arduino.h>
#include <vector>
#include <string>
#include <sstream>
using namespace std;

std::vector<string> split(const string &s, char delim);
int clampValue (int value, int min, int max);
